- name: install & start
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
    - name: nginx
      state: present
  - name: starting the nginx
    ansible.builtin.service:
     - name: nginx
       state: start
